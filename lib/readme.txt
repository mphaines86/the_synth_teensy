
This directory is where the project specific (private) libraries are which includes the_synth_due, which serves as the main synth engine and arduino_midi_library, which is used to handle midi protocal in the project and can be found over here: 
https://github.com/FortySevenEffects/arduino_midi_library.

PlatformIO compiles these libraries to static libraries and link to executable file.



